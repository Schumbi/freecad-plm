name: Test and Build FreeCAD PLM Image

on:
  push:
    branches:
      - main
      - master
  pull_request:
  workflow_dispatch:

env:
  REGISTRY: git.home.schumbi.de/ralf
  IMAGE_NAME: freecad-plm
  DOCKER_BUILDKIT: 1

jobs:
  build-image:
    runs-on: arch
    container:
      image: git.home.schumbi.de/ralf/debian-docker-builder:latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build image
        run: |
          IMAGE="${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}"
          docker build \
            --progress=plain \
            --pull=false \
            --build-arg BUILDKIT_INLINE_CACHE=1 \
            -t "${IMAGE}:latest" \
            -t "${IMAGE}:${{ github.sha }}" \
            .

      - name: Run tests
        run: |
          IMAGE="${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}"
          docker run --rm \
            -e DJANGO_DEBUG=1 \
            "${IMAGE}:${{ github.sha }}" \
            python manage.py test --parallel auto --verbosity=1

      - name: Login to Registry
        if: github.event_name != 'pull_request'
        run: |
          echo "${{ secrets.REGISTRY_PASSWORD }}" | docker login "${{ env.REGISTRY }}" \
            -u "${{ secrets.REGISTRY_USERNAME }}" \
            --password-stdin

      - name: Push image
        if: github.event_name != 'pull_request'
        run: |
          IMAGE="${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}"
          docker push "${IMAGE}:latest"
          docker push "${IMAGE}:${{ github.sha }}"
